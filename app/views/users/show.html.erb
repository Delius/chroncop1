<p id="notice"><%= notice %></p>

<div class="panel callout radius">
	<p>
		<strong>Name:</strong>
		<ul>

			
			<h2> <%= @user.name %></h2></ul>
				<%= gravatar @user %>		
				
			<h5>Tips to date (<%= @user.tips.count %>)</h5>
			<% if user_signed_in? %>
			<%= follow_button @user %>
			<% if current_user == @user %>
			<%= link_to 'Edit', edit_user_path(@user) %>
			<% if @user.tips.any? %>
			<% end %>
			<% end %>
		</div>

		<div >


			<%= render @tips  %>
<%= render @shouts %>
			<% end %>
		</div>

		<!--List Recently Created Events-->


</div>
